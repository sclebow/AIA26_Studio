# Fresh Start Archive

Archived on 2026-06-03 during the Team 04 Python-tool-first reset.

This snapshot preserves material that was removed from the Team 04 top level so the active folder could stay centered on:

- `agent/` as the only live runtime
- `agent/tools/` as the main development focus
- `tests/` and the current notebook harnesses as the main validation surface

## Archived Here

- `PY/`: extra live-looking Python workflow tree that was no longer the canonical runtime.
- `agent prototype/`: prototype exploration notebooks and assets.
- `reference_examples/JSON_Tools_Example/`: archived JSON tool example payloads that are not part of the active Team 04 runtime.
- `reference_examples/llm_call/`: archived LLM call example material that is not part of the active Team 04 runtime.
- `docs/QUICK_START.md`: older Grasshopper-first quick start.
- `docs/TERRAPILOT_PLAN.md`: older planning document.
- `docs/README_DELIVERABLES.md`: deliverables summary for the older plan.
- `docs/TOOLS_CHECKLIST.md`: old Grasshopper-first tool checklist.
- `docs/workflow_structure.ipynb` and `docs/workflow_diagram.png`: older workflow visualization artifacts.

No active Team 04 runtime path should depend on these archived files.