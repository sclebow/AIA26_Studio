Which other cool feature could I do, especially related to search?

0. Routine: improve system prompt and visualisation
1. select 2 layouts and search for "in between"
2. "Show me the opposite" 
3. visualise the graph of the layout
4. make the brief summary editable
5. allow the user to insert detailed routine 
6. dalyight visualisation?

add more voices in brief summary (adjacency, size)

show more miniatures and above the dots
decouple history?
improve adapt pipeline

BSCAN/k-means + cluster labeling + background regions on the SVG. Worth doing later, not a quick fix. For now the scatter plot is fine as a "technically correct" first version that your prof asked for.

================================================

1. Update reason.py prompt with new room names + add the missing graph fields to the JSON schema
2. Update evaluate.py prompt room categories
3. Update routine.py fallback to use circulation/storage/wc instead of extra/foyer
4. actually give multiple scores!