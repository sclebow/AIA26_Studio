# Drop the still screenshots here

The deck embeds these on slide 11 (Made real → the before/after). Exact filenames:

- `report-before.png` — a Report room render BEFORE an edit
- `report-after.png`  — the SAME room AFTER the edit (the "glow-up")

Until present, the deck shows labelled placeholders. Capture them from the Report's
before/after wipe (see `../../../demo-runbook-week09.md`).

Note: the close slide and the Grounded slide now use authored SVG visuals (no photo
needed), and slide 14 ("the renders, judged") uses the real benchmark renders already
bundled in `../`. So these two before/after stills are the only screenshots the deck needs.
