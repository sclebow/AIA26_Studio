# Drop the 3 demo clips here

The deck plays these full-bleed (un-numbered "▶ DEMO" pages). Exact filenames:

- `onboard.mp4` — Clip 1, ~50s — quiz → moodboard → persona reveal
- `shape.mp4`   — Clip 2, ~90s — CAD plan → scores → conflict → edit → Focus Pull → re-score → ripple graph → galaxy
- `report.mp4`  — Clip 3, ~70s — persona + moodboard return → renders → before/after

Recording path + de-risking: see `../../../demo-runbook-week09.md`. Silent screen-captures
(you narrate live). MP4 (H.264), 1080p. Until a file is present, its page shows a placeholder.
